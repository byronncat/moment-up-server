<!DOCTYPE html>
<html lang='en'>

<head>
  <meta charset='UTF-8' />
  <meta name='viewport' content='width=device-width, initial-scale=1.0' />
  <title>{{title}} - {{#if (eq templateType "success")}}Email Verified Successfully{{else}}Verification Failed{{/if}}</title>
  <style>
    /* Reset styles */
    body, h1, h2, p {
      margin: 0;
      padding: 0;
      border: 0;
    }
    
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: #333333;
      margin: 0;
      padding: 0;
      width: 100%;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    
    .success-container {
      background-color: #ffffff;
      border-radius: 20px;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
      padding: 60px 40px;
      text-align: center;
      max-width: 500px;
      width: 90%;
      margin: 20px;
    }
    
    .logo-section {
      margin-bottom: 30px;
    }
    
    .logo-container {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 20px;
    }
    
    .logo-img {
      width: 40px;
      height: 40px;
      margin-right: 12px;
    }
    
    .brand-text {
      color: hsl(341 92% 60%);
      font-size: 28px;
      font-weight: bold;
      text-decoration: none;
    }
    
    /* Success Icon Styles */
    .success-icon {
      width: 80px;
      height: 80px;
      background: linear-gradient(135deg, #4CAF50, #45a049);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 30px auto;
      box-shadow: 0 10px 30px rgba(76, 175, 80, 0.3);
      animation: bounce 0.6s ease-out;
    }
    
    .check-mark {
      color: white;
      font-size: 40px;
      font-weight: bold;
    }
    
    /* Failure Icon Styles */
    .failure-icon {
      width: 80px;
      height: 80px;
      background: linear-gradient(135deg, #f44336, #d32f2f);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 30px auto;
      box-shadow: 0 10px 30px rgba(244, 67, 54, 0.3);
      animation: shake 0.6s ease-out;
    }
    
    .error-mark {
      color: white;
      font-size: 40px;
      font-weight: bold;
    }
    
    /* Success Text Styles */
    .success-title {
      font-size: 32px;
      color: #2b303a;
      margin: 0 0 16px 0;
      font-weight: 700;
    }
    
    .success-description {
      font-size: 16px;
      color: #666;
      line-height: 1.6;
      margin: 0 0 40px 0;
      max-width: 400px;
      margin-left: auto;
      margin-right: auto;
    }
    
    /* Failure Text Styles */
    .failure-title {
      font-size: 32px;
      color: #d32f2f;
      margin: 0 0 16px 0;
      font-weight: 700;
    }
    
    .failure-description {
      font-size: 16px;
      color: #666;
      line-height: 1.6;
      margin: 0 0 30px 0;
      max-width: 400px;
      margin-left: auto;
      margin-right: auto;
    }
    
    /* Button Styles */
    .cta-button {
      display: inline-block;
      background: linear-gradient(135deg, #d00cf7, #960be6);
      background-color: #d00cf7;
      padding: 16px 32px;
      border-radius: 50px;
      box-shadow: 0 8px 25px rgba(247, 12, 169, 0.3);
      color: white !important;
      font-size: 16px;
      font-weight: 600;
      text-decoration: none;
      transition: all 0.3s ease;
      border: none;
      cursor: pointer;
      margin: 0 8px 16px 8px;
    }
    
    .cta-button:hover {
      background: linear-gradient(135deg, #e01cff, #a60df0);
      box-shadow: 0 12px 35px rgba(247, 12, 169, 0.4);
      transform: translateY(-2px);
    }
    
    .cta-button:active {
      transform: translateY(0px);
    }
    
    .cta-button.secondary {
      background: linear-gradient(135deg, #6c757d, #5a6268);
      background-color: #6c757d;
      box-shadow: 0 8px 25px rgba(108, 117, 125, 0.3);
    }
    
    .cta-button.secondary:hover {
      background: linear-gradient(135deg, #7c858d, #6a7278);
      box-shadow: 0 12px 35px rgba(108, 117, 125, 0.4);
      transform: translateY(-2px);
    }
    
    .button-group {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 8px;
      margin-bottom: 30px;
    }
    
    /* Footer and Support Styles */
    .footer-text {
      margin-top: 40px;
      font-size: 14px;
      color: #999;
    }
    
    .support-info {
      margin-top: 30px;
      padding: 20px;
      background-color: #f8f9fa;
      border-radius: 12px;
      border: 1px solid #e9ecef;
    }
    
    .support-text {
      font-size: 14px;
      color: #666;
      margin: 0;
    }
    
    .support-link {
      color: #d00cf7;
      text-decoration: none;
      font-weight: 600;
    }
    
    .support-link:hover {
      text-decoration: underline;
    }
    
    /* Animations */
    @keyframes bounce {
      0% {
        opacity: 0;
        transform: scale(0.3);
      }
      50% {
        opacity: 1;
        transform: scale(1.05);
      }
      70% {
        transform: scale(0.9);
      }
      100% {
        opacity: 1;
        transform: scale(1);
      }
    }
    
    @keyframes shake {
      0% {
        opacity: 0;
        transform: translateX(0);
      }
      25% {
        opacity: 1;
        transform: translateX(-5px);
      }
      50% {
        transform: translateX(5px);
      }
      75% {
        transform: translateX(-5px);
      }
      100% {
        opacity: 1;
        transform: translateX(0);
      }
    }
    
    /* Responsive design */
    @media (max-width: 768px) {
      .success-container {
        padding: 40px 30px;
        margin: 10px;
      }
      
      .success-title,
      .failure-title {
        font-size: 28px;
      }
      
      .success-description,
      .failure-description {
        font-size: 15px;
      }
      
      .cta-button {
        padding: 14px 28px;
        font-size: 15px;
      }
      
      .button-group {
        flex-direction: column;
        align-items: center;
      }
      
      .cta-button {
        width: 200px;
        margin: 8px 0;
      }
    }
    
    @media (max-width: 480px) {
      .success-container {
        padding: 30px 20px;
      }
      
      .success-title,
      .failure-title {
        font-size: 24px;
      }
      
      .success-icon,
      .failure-icon {
        width: 70px;
        height: 70px;
      }
      
      .check-mark,
      .error-mark {
        font-size: 35px;
      }
    }
  </style>
</head>

<body>
  <div class="success-container">
    <!-- Logo Section -->
    <div class="logo-section">
      <div class="logo-container">
        <img src="{{logoUrl}}" alt="{{brandName}} Logo" class="logo-img" />
        <span class="brand-text">{{brandName}}</span>
      </div>
    </div>

    <!-- Content Based on Type -->
    {{#if (eq templateType "success")}}
      {{> success-verification}}
    {{else if (eq templateType "failure")}}
      {{> failure-verification}}
    {{else}}
      {{> success-verification}}
    {{/if}}
  </div>
</body>

</html>