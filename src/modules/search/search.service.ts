import { Injectable } from '@nestjs/common';

@Injectable()
export class SearchService {
  // TODO: Implement search methods
  // - searchUsers(query, userId)
  // - searchMoments(query, userId)
  // - searchHashtags(query)
  // - searchAll(query, userId)
}
